<form method="post" action="{{ path('field_of_work_delete', {'id': field_of_work.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ field_of_work.id) }}">
    <button class="btn">Delete</button>
</form>
