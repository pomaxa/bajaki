{% extends 'base.html.twig' %}

{% block title %}FieldOfWork{% endblock %}

{% block body %}
    <h1>FieldOfWork</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ field_of_work.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ field_of_work.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('field_of_work_index') }}">back to list</a>

    <a href="{{ path('field_of_work_edit', {'id': field_of_work.id}) }}">edit</a>

    {{ include('field_of_work/_delete_form.html.twig') }}
{% endblock %}
