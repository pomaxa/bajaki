{% extends 'base.html.twig' %}

{% block title %}KnowFrom{% endblock %}

{% block body %}
    <h1>KnowFrom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ know_from.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ know_from.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('know_from_index') }}">back to list</a>

    <a href="{{ path('know_from_edit', {'id': know_from.id}) }}">edit</a>

    {{ include('know_from/_delete_form.html.twig') }}
{% endblock %}
